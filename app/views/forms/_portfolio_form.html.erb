<!-- this is the unfinished portfolio page -->

<% form_for @new_portfolio, :html => { :multipart => true } do |f| %>
	<%= f.file_field :photo %><br/>
	<%= f.text_field :url%>      
	<%= f.submit "Submit" %>
<% end %>
